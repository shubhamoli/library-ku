{
  "name": "Backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.12",
    "nodemon": "^2.0.7",
    "path": "^0.12.7"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd .. && cd client && npm run build",
    "install_client": "cd .. && cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "client": "cd .. && cd client && npm start",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
